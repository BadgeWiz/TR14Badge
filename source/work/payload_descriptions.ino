int pl_amount=15;

void sprint_payloads(){
  
      Serial.println(F("Overview of all currently coded Payload Scripts"));
      Serial.println("");
      Serial.println(F("1. Lock, Windows, Leaves message and locks the screen"));
      Serial.println(F("2. Lock, Mac OSX, Leaves message and locks the screen"));
      Serial.println(F("3. Lock, GnuLinux, Leaves message and locks the screen"));
      Serial.println(F("4. Call Agena, Windows, Opens TROOPERS 14 Agenda in browser"));
      Serial.println(F("5. Call Agena, Mac OSX, Opens TROOPERS 14 Agenda in browser"));
      Serial.println(F("6. Call Agena, GnuLinux, Opens TROOPERS 14 Agenda in browser"));
      /*excluded
      Serial.println(F("7. Show WiFi PWs, Windows, Uses powershell to show stored WiFi PWs"));
      Serial.println(F("8. Force Creds, Windows, Uses powershell to force input of valid Win credentials"));
      Serial.println(F("9. Lockscreen Backdoor, Windows, Swaps Ease of Access screen against console"));
      Serial.println(F("10. Rev MetPret, Windows, Download and execute Meterpreter (need to adjust ip in source!)"));
      Serial.println(F("11. Add user with net.exe, Windows, Add user: Owned 0wn3d using net.exe"));
      Serial.println(F("12. Add user with vbs, Windows, Add user: Owned 0wn3d using VBscript, no net.exe"));
      Serial.println(F("13. Shutdown, Windows, Shutdown immediately"));
      Serial.println(F("14. N Calc, Windows, Start Calc.exe 100times"));
      Serial.println(F("15. Send Mail, Windows, Send mail using mailto:"));
      */
      
}
